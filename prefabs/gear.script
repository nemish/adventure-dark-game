go.property("speed", 5)
go.property("direction", hash("left"))

function init(self)
    z = go.get(".", "euler.z")
    multiplier = 1
    if self.direction == hash("left") then
        multiplier = -1
    end
    -- go.animate(".", "euler.z", go.PLAYBACK_LOOP_FORWARD, z + (360 * multiplier), go.EASING_LINEAR, self.speed)
end
